#!/bin/bash

# Add all changes
git add .

# Commit changes with the provided message
git commit -m "xxxxxxxx"

# Push changes to the main branch on the remote repository named 'origin'
git push origin main

